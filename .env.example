# Shared MongoDB database for the Next.js API routes.
# Use the same database as auth-server, but supply this value to the Next.js process.
MONGODB_URI=mongodb://127.0.0.1:27017/adtu_rag_auth

# Existing server-side RAG endpoint.
RAG_API_URL=http://localhost:8000
